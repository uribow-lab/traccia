# セキュリティについて

Traccia は手元で動かす個人向けのツールです。サーバーに何かを預ける仕組みはありません。
それでも **API キー** と **素材（動画・音声・書き起こし）** の 2 つは扱いを間違えると
実害が出るので、ここに 1 枚でまとめます。

キーの取得手順そのものは [docs/api-keys.md](docs/api-keys.md) にあります。

---

## 1. Traccia が扱う秘密

| 何 | どこから来るか | 何に使うか |
|---|---|---|
| `GEMINI_API_KEY` | 環境変数 または エディタの設定画面 | Gemini での文字起こし（**従量課金**） |
| `HF_TOKEN` | コマンドライン引数 `--hf-token` または環境変数 | pyannote のモデル取得（話者分離）。課金なし |
| `DEEPGRAM_API_KEY` / `ASSEMBLYAI_API_KEY` | 環境変数 | 検証用スクリプト（`bench/`。本体には含まれません） |

**字幕の編集だけなら、キーは 1 つも要りません。**

---

## 2. キーはどこに保存されるか

エディタの設定画面から入れた Gemini のキーは、**リポジトリの外**に置かれます。

```
~/.traccia/config.json     # API キー・モデル・今月の上限        パーミッション 0600
~/.traccia/usage.jsonl     # 1 回ぶんの実測トークンと概算額
```

- **0600**（本人だけが読み書きできる）で書きます。
- 置き場所を変えたいときは環境変数 `TRACCIA_HOME` を指定してください。

### 素材フォルダには置かない

`resources/` の中にキーを書いたファイルを置かないでください。
素材は「動画ごと誰かに渡す」ことがあります。そのときキーまで一緒に渡ってしまいます。
`~/.traccia/` に分けているのは、まさにこの事故を防ぐためです。

### 環境変数で渡すときの注意

`GEMINI_API_KEY` は環境変数からも読みます。シェルの起動ファイル（`.zshrc` 等）に
書く場合、そのファイルを別リポジトリで管理しているなら、そちらの公開設定も確認してください。

---

## 3. コミットしないもの

`.gitignore` で以下を除外しています。

| 除外しているもの | 理由 |
|---|---|
| `.env` / `.env.*`（`.env.example` を除く） | Jira・Chatwork のトークン |
| `mac_command.txt` / `win_実行コマンド.txt` | 手元の起動コマンド控え。HuggingFace トークンを直書きしがち |
| `*.token` / `hf_token*` | 同上 |
| `resources/` | 素材動画と、その書き起こし |
| `bench/` | 検証結果に素材の書き起こしがそのまま残る |
| `docs-local/` | 手元用のメモ |
| `*.mp4` / `*.mov` / `*.wav` ほか | 置き場所を問わず動画・音声を除外 |

> **除外設定は事故で外れます。最後の砦にしないでください。**
> キーは常に「ファイルに書かない・環境変数か `~/.traccia/config.json` で渡す」を守る方が確実です。

`.env.example` だけは、キー名の見本として意図的に追跡しています。
**ここに値を書かないでください。**

---

## 4. 素材そのものの扱い

文字起こしを外部サービス（Gemini / Deepgram / AssemblyAI）に任せる経路では、
**音声がそのまま送信されます。** 会議・インタビュー・未公開の収録など、
外に出せない内容を含む素材では、その経路を使わないでください。

ローカル経路（`faster-whisper` + `pyannote`）は手元で完結します。
`HF_TOKEN` はモデルを**ダウンロードする鍵**であって、音声の送り先ではありません。

---

## 5. キーが漏れたと思ったら

**漏れた可能性がある時点で、そのキーは使えないものとして扱ってください。**
確認するより、消して作り直す方が速く、確実です。

| | 失効させる場所 |
|---|---|
| Gemini | <https://aistudio.google.com/app/apikey> → 対象を削除 → 作り直す |
| HuggingFace | <https://huggingface.co/settings/tokens> → 対象を Delete → 作り直す |
| Deepgram | コンソール → API Keys → 対象を削除 → 作り直す |
| AssemblyAI | ダッシュボード → キーのローテーション |

Gemini / Deepgram / AssemblyAI は**使った分だけ課金される**ので、放置すると請求につながります。

うっかりコミットしてしまった場合は、**キーの失効を先に**やってください。
履歴から消す作業（`git filter-repo` 等）は、その後で構いません。
push 済みなら、履歴を書き換えても取得された可能性は消せません。

---

## 6. 脆弱性を見つけたら

**公開 Issue には書かないでください。**

GitHub の **Security** タブ → **Report a vulnerability** から、非公開で報告できます。
（[Privately reporting a security vulnerability](https://docs.github.com/code-security/security-advisories/guidance-on-reporting-and-writing-information-about-vulnerabilities/privately-reporting-a-security-vulnerability)）

報告に入れてほしいもの:

- 何が起きるか（想定される被害）
- 再現手順
- 影響する範囲（OS / Python のバージョン / どのサブコマンド）

個人が趣味で保守しているツールです。**返信は遅いことがあります。**
SLA はありません。緊急の対応が必要な用途では使わないでください。

---

## 7. サポート範囲

このツールにセキュリティ更新の保証はありません。最新のコミットのみが対象で、
過去のバージョンへのバックポートは行いません。
