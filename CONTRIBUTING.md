# このリポジトリへの関わり方

先に正直なところを書きます。

**Traccia は、自分の作業を楽にするために作った個人ツールです。**
公開しているのは「同じことで困っている人が居たら使えるかもしれない」からで、
共同開発を募っているわけではありません。

そのうえで、次のように考えています。

| | 歓迎 | 理由 |
|---|---|---|
| **バグ報告** | ⭕️ とても歓迎 | 手元の環境（Intel Mac / Windows）でしか試せていません。他の環境で壊れているところは、教えてもらわないと分かりません |
| **ドキュメントの誤り・分かりにくさの指摘** | ⭕️ 歓迎 | 書いた本人には抜けが見えません |
| **小さな修正の PR**（typo・明らかな不具合） | ⭕️ 歓迎 | そのまま取り込めます |
| **機能追加の PR** | ❌ 基本的に受けていません | 下記 |
| **リファクタリング・構成変更の PR** | ❌ 受けていません | 同上 |

## 機能追加の PR を受けていない理由

保守できる量を超えるからです。
このツールは一人で使うことを前提に、判断の基準（何を保存するか、何を保存しないか、
どこまで自動でやるか）を細かく決めてあります。他の人の使い方に合わせて広げていくと、
その基準が崩れて、自分でも直せないものになります。

**要望として Issue に書いてもらうのは歓迎です。** 同じ困りごとが自分にもあれば作ります。
ただし「いつ作るか」「作るかどうか」は約束できません。

先に PR を書いてしまうと、それが無駄になります。**手を動かす前に Issue で聞いてください。**

## バグ報告のしかた

[Issue](../../issues/new/choose) の「バグ報告」から。

再現に必要なので、以下を書いてください（テンプレートに項目があります）。

- **OS**（macOS は Intel か Apple Silicon かも）
- **Python のバージョン**（`python3 --version`）
- **どの機能か**（エディタ / 文字起こし / Gemini / `wfp`）
- **素材の形式**（mp4 か mov か、長さ、音声のチャンネル数）
- **セットの構成**（`resources/<名前>/` に何が入っているか）

**貼らないでください:**

- API キー（`hf_…` / `AIza…` など）
- 素材そのもの（動画・音声）
- 実際の書き起こし本文

ログを貼るときは、キーらしき文字列を消してから貼ってください。
セキュリティ上の問題は Issue ではなく [SECURITY.md](SECURITY.md) の手順で報告してください。

## 小さな修正の PR を出す場合

1. Issue を立てるか、既存の Issue にコメントしてから始めてください（重複を避けるため）
2. 1 つの PR で 1 つのことだけ直してください
3. 動かして確認してから出してください。テストは置いていないので、確認は手元で通す形になります
4. [PR テンプレート](.github/PULL_REQUEST_TEMPLATE.md)の「確認項目」に、実際に通した手順のチェックを入れてください

### 手元での動かし方

```bash
python3.12 -m venv .venv
.venv/bin/python -m pip install -r requirements.txt   # 文字起こしまで試すなら
.venv/bin/python -m traccia edit                      # 編集だけなら依存はほぼ不要
```

編集側（`traccia/editor/`）は標準ライブラリ + PyAV だけで動きます。
詳しくは [README.md](README.md) の「セットアップ」を見てください。

### 気をつけていること

- **保存形式の互換を壊さない。** `.edit.json` / `.settings.json` は、
  すでに手元に溜まっている作業データです。読めなくなると作業が消えます
- **素材を書き換えない。** 元の `.srt` と動画には触りません
- **依存を増やさない。** 編集側は標準ライブラリで済ませています

## 返信について

趣味で保守しているので、**返信は遅いことがあります。** 反応が無くても催促は不要です。
読んではいます。
