# ============================================================
# Traccia — 話者別字幕ツール
# ============================================================

# --- 資格情報 ------------------------------------------------
# HuggingFace トークンが平文で書かれている。絶対にコミットしない。
mac_command.txt
win_実行コマンド.txt
*.token
hf_token*

# .env は派生形（.env.local / .env.production など）も含めて全部除外する。
# 中身が空の見本だけは、置きたくなったときのために通す。
.env
.env.*
!.env.example

# --- 仮想環境（各 1.4GB）------------------------------------
# requirements.txt から作り直せる
.venv/
venv/
venv.backup/
env/

# --- Python --------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- 素材（動画 1本で 600〜900MB）---------------------------
# 動画も字幕も手元だけで持つ。Git に入れるものではない。
resources/

# 字幕だけは版を残したい、という場合は上の resources/ を消して
# 代わりに以下を使う（動画とツールの生成物だけ除外する）
# resources/**/*.mp4
# resources/**/*.mov
# resources/**/*.mkv
# resources/**/*.m4v
# resources/**/*.webm
# resources/**/*.wav
# resources/**/*.m4a

# --- Traccia が作るファイル ---------------------------------
# いずれも元の .srt から作り直せる。原本ではない。
*.edit.json          # 編集中のデータ
*.settings.json      # 話者の名前と色
*.peaks.json         # PCM 波形のピーク
*.json.tmp           # 保存中の一時ファイル
*.json.[0-9]         # 世代バックアップ（.edit.json.1 など）
backup/              # 世代バックアップの置き場
export/              # 書き出した話者別 .srt

# --- 動画・音声（置き場所を問わず）-------------------------
*.mp4
*.mov
*.mkv
*.m4v
*.webm
*.wav
*.m4a
*.mp3

# --- 検証用（API 比較など。結果に音声の書き起こしが入る）-----
bench/

# --- 手元用のメモ・下書き（公開しない）------------------------
docs-local/

# --- Claude Code の手元設定 ---------------------------------
# 許可したコマンドや参照先のパスが入る。グローバルの gitignore で
# 除外していても、環境が変わればそのまま追跡されてしまうのでここでも書く。
.claude/settings.local.json

# --- ログ・作業ファイル -------------------------------------
*.log
run.log
*.bak
*.orig
*.tmp

# --- モデルのキャッシュ -------------------------------------
# 通常は ~/.cache に入るが、環境変数で手元に向けたとき用
.cache/
models/
*.bin
*.ct2

# --- macOS ---------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# --- Windows -------------------------------------------------
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# --- エディタ / IDE ------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
